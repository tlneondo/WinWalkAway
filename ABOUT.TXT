About:

Just a quick tool to make dualbooting easier

currently uses grub2 as that's whats in fedora

We have a script that runs,

Replace ~/Scripts with whatever the path to the script is


alias wreboot="pkexec ~/Scripts/winwalkaway.sh" to your bashrc or zshrc
